const STORAGE_KEYS = {
  FILTER_UNWATCHED: {
    MY_LIST: "filter_unwatched_my_list",
    MOVIES_TO_ME: "filter_unwatched_movies_to_me",
    OUR_MOVIES: "filter_unwatched_our_movies",
  },
} as const;

export default STORAGE_KEYS;
